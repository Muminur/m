{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "floating-recorder",
  "description": "Least-privilege IPC capability for the floating recorder webview",
  "windows": ["float"],
  "permissions": ["core:default"]
}
